# Steps to run tests:
1. Ensure python and chromedriver are installed
2. Virtual env: python3 -m venv selenium_env
source selenium_env/bin/activate
3. Run command `pip3 install -r requirements.txt`
4. Run tests with command `pytest serve_ui_tests.py`

